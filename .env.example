# KALA Frontend Environment Configuration
# Safe development defaults (DO NOT commit real production values or secrets)

# Backend API Base URL (defaults to http://localhost:5000 in development if omitted)
VITE_API_BASE_URL=http://localhost:5000
